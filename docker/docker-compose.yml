services:
  mongodb:
    image: mongo:8.0.21
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - "./mongodb:/data/db"
    networks:
      - backend

networks:
  backend:
    driver: bridge